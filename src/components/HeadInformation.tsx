// src/components/HeadInformation.tsx
'use client';

import React from 'react';

// This component can be used to add specific head elements
// to a page, though general site metadata is in RootLayout.
// For now, it's a placeholder to resolve import errors.
export default function HeadInformation() {
  return (
    <>
      {/* Add any page-specific head tags here if needed */}
      {/* Example: <meta name="keywords" content="shift, schedule" /> */}
    </>
  );
}
